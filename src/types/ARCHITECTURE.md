# types — Architecture

> Local proximity doc. Module-specific only; shared boundaries and rules are not
> restated here — follow the links.

## Purpose

Shared TypeScript types and interfaces used across modules. The first file
created when scaffolding a new feature.

## Key files & structure

- _Add domain type modules here (e.g. `billing.ts`, `user.ts`)._

## Local notes

- Type-only; no runtime code, no side effects; named exports.

## Related (canonical)

- "Native types, no JSDoc types, no `any`" → `CONSTRAINTS.md`
- Feature creation order → `docs/AGENT_OPS.md`
