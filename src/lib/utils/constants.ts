// App configuration has moved to @/config
// This file is kept for backward compatibility — re-exports from @/config
export {
  siteConfig,
  routes,
  apiConfig,
  httpStatus,
  pagination,
  storageKeys,
  queryKeys,
} from '@/config'
